# Capstone Project — E-Commerce, Real Estate & Churn

## Overview
End-to-end project: EDA, predictive models (house price, churn, sales), Streamlit app, Docker & deployment-ready.

## Quickstart (local)
1. Create `.env` in project root (see `.env.example` below or use default settings).
2. Create and activate venv:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
